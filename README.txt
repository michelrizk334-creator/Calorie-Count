Calorie Count v30.3 — Readable Wrapped Tabs

Minor UI-only update on top of v30.2.
- Keeps all five top tabs equal width.
- Restores a larger, more readable font.
- Allows long tab names such as 'Support List' to wrap onto two centered lines.
- Centers labels horizontally and vertically inside each tab.
- Everything else is unchanged.
- Service-worker cache bumped to v30.3.

Upload the root contents to GitHub and commit. No Cloudflare change is required.
